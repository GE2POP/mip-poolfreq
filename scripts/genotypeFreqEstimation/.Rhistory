n<-120
FisVrai <- (1-t)/(1+t)
Sim <-c()
for (i in 1:10000) {
A1<-rbinom(n,1,p)
HomoZ <- rbinom(n, 1, FisVrai)
A2 <- A1 * (HomoZ==1) +  rbinom(1, 1,0.5)*(HomoZ==0)
G<-cbind(A1, A2)
pprim<-sum(G)/(ncol(G)*nrow(G))
qprim<-1-pprim
HeAtt=2*pprim*qprim
GOBS<-as.data.frame(G)
HeObs <- sum((GOBS$A1 + GOBS$A2)==1)/nrow(G)
Fis<-1-HeObs/HeAtt
Sim[i]<-Fis }
hist(Sim, main = paste0("Distribution of FIS values for t = ", t),
xlab = "FIS", ylab = "Count", xlim=c(0.75, 1))
abline(v = mean(Sim), col = "darkblue", lwd = 2)
text(mean(Sim)-0.035, 2000, paste0("Mean FIS = ", round(mean(Sim), 2)), col = "darkblue")
abline(v = 0.8, col = "darkred", lwd = 2)
text(0.8+0.03, 300, paste0("0.8 threshold"), col = "darkred")
mean(Sim)-2*sd(Sim)
t
p<-0.5
q<-1-p
t <-0.05
n<-120
FisVrai <- (1-t)/(1+t)
Sim <-c()
for (i in 1:10000) {
A1<-rbinom(n,1,p)
HomoZ <- rbinom(n, 1, FisVrai)
A2 <- A1 * (HomoZ==1) +  rbinom(1, 1,0.5)*(HomoZ==0)
G<-cbind(A1, A2)
pprim<-sum(G)/(ncol(G)*nrow(G))
qprim<-1-pprim
HeAtt=2*pprim*qprim
GOBS<-as.data.frame(G)
HeObs <- sum((GOBS$A1 + GOBS$A2)==1)/nrow(G)
Fis<-1-HeObs/HeAtt
Sim[i]<-Fis }
hist(Sim, main = paste0("Distribution of FIS values for t = ", t),
xlab = "FIS", ylab = "Count", xlim=c(0.75, 1))
abline(v = mean(Sim), col = "darkblue", lwd = 2)
text(mean(Sim)-0.035, 2000, paste0("Mean FIS = ", round(mean(Sim), 2)), col = "darkblue")
abline(v = 0.8, col = "darkred", lwd = 2)
text(0.8+0.03, 300, paste0("0.8 threshold"), col = "darkred")
mean(Sim)-2*sd(Sim)
t
mypath<-"~/Investigations_MIPS/TEST_0424/freq_files"
filenames<-list.files(path=mypath, full.names=TRUE)
mergedFile<-read.table(filenames[1], header=T)
for (filename in filenames[-1]){
mergedFile<-merge(mergedFile, read.table(filename, header=T), by=c("Chr","Pos"), all=T)
}
library(rsconnect)
rsconnect::deployApp('~/Autres/shiny_app_duplicats/full_app')
rsconnect::deployApp('~/Autres/shiny_app_duplicats/full_app_separated')
library(shiny); runApp('Autres/shiny_app_duplicats/full_app/app_avec_offT.R')
library(shiny); runApp('Autres/shiny_app_duplicats/full_app/app_avec_offT.R')
sample(5)
sample(1:4, 3)
sample(1:4, 3)
sample(1:4, 3, replace=T)
packages <- c(
"tidyr", "dplyr","psych", "reshape", "ggplot2", "plotly",
"gridExtra", "kableExtra",
"extraDistr", "pwr",
"FactoMineR", "factoextra",
"chillR", "pls", "MuMIn", "ggrepel",
"glmnet", "lmridge", "boot",
"HDCI", "glmnetSE")                     # Liste des packages à charger
# Installe les packages si ils ne sont pas déjà installés.
packages_check <- lapply(
packages,
FUN = function(x){
if (!require(x, character.only = TRUE)){
install.packages(x, dependencies = TRUE)
}
}
)
packages <- c(
"tidyr", "dplyr","psych", "reshape", "ggplot2", "plotly",
"gridExtra", "kableExtra",
"extraDistr", "pwr",
"FactoMineR", "factoextra",
"chillR", "pls", "MuMIn", "ggrepel",
"glmnet", "lmridge", "boot",
"HDCI", "glmnetSE")                     # Liste des packages à charger
# Installe les packages si ils ne sont pas déjà installés.
packages_check <- lapply(
packages,
FUN = function(x){
if (!require(x, character.only = TRUE)){
install.packages(x, dependencies = TRUE)
}
}
)
warnings()
for (pkg in packages){
library(pkg)
}
packages
for (pkg in packages){
print(pkg)
}
for (pkg in packages){
library(pkg)
}
library("tidyr")
for (pkg in packages){
library(pkg, character.only = TRUE)
}
library("chillR")
for (pkg in packages){
library(pkg, character.only = TRUE)
}
install.packages("MuMIn")
genotypeFreqEstimation_path="~/MIPs/PUBLI/03_scripts/genotypeFreqEstimation_old"
genotyping_vcf_path=paste0(genotypeFreqEstimation_path, "/tests/data/GENOTYPING_MATRIX/04__Genotype_Locus1_Sample_Locus2_Filtered.vcf")
vcf <- read.vcfR(genotyping_vcf_path)
livrary("vcfR")
genotypeFreqEstimation_path="~/MIPs/PUBLI/03_scripts/genotypeFreqEstimation_old"
genotyping_vcf_path=paste0(genotypeFreqEstimation_path, "/tests/data/GENOTYPING_MATRIX/04__Genotype_Locus1_Sample_Locus2_Filtered.vcf")
vcf <- read.vcfR(genotyping_vcf_path)
library("vcfR")
genotypeFreqEstimation_path="~/MIPs/PUBLI/03_scripts/genotypeFreqEstimation_old"
genotyping_vcf_path=paste0(genotypeFreqEstimation_path, "/tests/data/GENOTYPING_MATRIX/04__Genotype_Locus1_Sample_Locus2_Filtered.vcf")
vcf <- read.vcfR(genotyping_vcf_path)
gt_matrix <- extract.gt(vcf, element = "GT", element="TGT")
gt_matrix <- extract.gt(vcf, element="TGT")
View(gt_matrix)
# v ... to replace with CLI params ... v
## Input parameters
genotypeFreqEstimation_path="~/GitHub/mip-poolfreq/scripts/genotypeFreqEstimation"
depth_list=paste0(genotypeFreqEstimation_path, "/data_test/data/ALL_FREQ/depth_files.list")
hline=50
#output_file_name=paste0(genotypeFreqEstimation_path, "/data_test/results/estimate_MIPs_efficiency/depth_per_marker_boxplots.png")
out_dir=paste0(genotypeFreqEstimation_path, "/tests/results/estimate_MIPs_efficiency")
## Import dependencies
user_lib <- Sys.getenv("R_LIBS_USER")
if (!dir.exists(user_lib)) dir.create(user_lib, recursive = TRUE)
.libPaths(user_lib)
pkgs<-c(
"optparse",
"devtools",
"glue",
"ggplot2",
"reshape2"
)
for (pkg in pkgs) {
if (!requireNamespace(pkg, quietly = TRUE)) {
message("Installing missing package: ", pkg)
install.packages(pkg, dependencies = TRUE)
}
library(pkg, character.only = TRUE)
}
script_dir <- dirname(this.path::this.path())
project_root <- normalizePath(file.path(script_dir, ".."))
setwd(project_root)
load_all(".")
required_files <- list(
depth_list = depth_list
)
optional_files <- list(
)
check_missing_args(args = c(required_files, output_file_name = output_file_name))
# v ... to replace with CLI params ... v
## Input parameters
genotypeFreqEstimation_path="~/GitHub/mip-poolfreq/scripts/genotypeFreqEstimation"
depth_list=paste0(genotypeFreqEstimation_path, "/data_test/data/ALL_FREQ/depth_files.list")
hline=50
#output_file_name=paste0(genotypeFreqEstimation_path, "/data_test/results/estimate_MIPs_efficiency/depth_per_marker_boxplots.png")
out_dir=paste0(genotypeFreqEstimation_path, "/tests/results/estimate_MIPs_efficiency")
## Import dependencies
user_lib <- Sys.getenv("R_LIBS_USER")
if (!dir.exists(user_lib)) dir.create(user_lib, recursive = TRUE)
.libPaths(user_lib)
pkgs<-c(
"optparse",
"devtools",
"glue",
"ggplot2",
"reshape2"
)
for (pkg in pkgs) {
if (!requireNamespace(pkg, quietly = TRUE)) {
message("Installing missing package: ", pkg)
install.packages(pkg, dependencies = TRUE)
}
library(pkg, character.only = TRUE)
}
script_dir <- dirname(this.path::this.path())
project_root <- normalizePath(file.path(script_dir, ".."))
setwd(project_root)
load_all(".")
required_files <- list(
depth_list = depth_list,
out_dir = out_dir
)
optional_files <- list(
)
check_missing_args(args = required_files)
check_input_files(
required_files = required_files,
optional_files = optional_files
)
#output_file_name=paste0(genotypeFreqEstimation_path, "/data_test/results/plot_depth_per_marker/depth_per_marker_boxplots.png")
out_dir=paste0(genotypeFreqEstimation_path, "/tests/results/plot_depth_per_marker")
required_files <- list(
depth_list = depth_list,
out_dir = out_dir
)
check_input_files(
required_files = required_files,
optional_files = optional_files
)
#output_file_name=paste0(genotypeFreqEstimation_path, "/data_test/results/plot_depth_per_marker/depth_per_marker_boxplots.png")
out_dir=paste0(genotypeFreqEstimation_path, "/data_test/results/plot_depth_per_marker")
required_files <- list(
depth_list = depth_list,
out_dir = out_dir
)
check_input_files(
required_files = required_files,
optional_files = optional_files
)
## Plot depths per marker
depths<-merge_depth_files(
depth_list = depth_list
)
depths <- clean_depth_file(depths)
View(depths)
rowMeans(depths)
t(rowMeans(depths))
t(t(rowMeans(depths)))
as.data.frame(rowMeans(depths))
as.data.frame(cbind(row.names(depths),rowMeans(depths)))
mean_depths<-as.data.frame(cbind(row.names(depths),rowMeans(depths)))
colnames(mean_depths)<-c("CHROM_POS", "mean_depth")
View(mean_depths)
write_mean_depth_per_marker <- function(depths, out_file){
depths <- clean_depth_file(depths)
mean_depths<-as.data.frame(cbind(row.names(depths),rowMeans(depths)))
colnames(mean_depths)<-c("CHROM_POS", "mean_depth")
write.table(mean_depths, out_file, row.names = FALSE, quote = F, sep ="\t")
}
write_mean_depth_per_marker(
depths=depths,
out_file=glue("{out_dir}/mean_depth_per_marker.tsv")
)
depths
## Plot depths per marker
depths<-merge_depth_files(
depth_list = depth_list
)
write_mean_depth_per_marker(
depths=depths,
out_file=glue("{out_dir}/mean_depth_per_marker.tsv")
)
round(rowMeans(depths), 2)
## Plot depths per marker
depths<-merge_depth_files(
depth_list = depth_list
)
depths <- clean_depth_file(depths)
round(rowMeans(depths), 2)
write_mean_depth_per_marker <- function(depths, out_file){
depths <- clean_depth_file(depths)
mean_depths<-as.data.frame(cbind(row.names(depths),round(rowMeans(depths), 2)))
colnames(mean_depths)<-c("CHROM_POS", "mean_depth")
write.table(mean_depths, out_file, row.names = FALSE, quote = F, sep ="\t")
}
write_mean_depth_per_marker(
depths=depths,
out_file=glue("{out_dir}/mean_depth_per_marker.tsv")
)
## Plot depths per marker
depths<-merge_depth_files(
depth_list = depth_list
)
write_mean_depth_per_marker(
depths=depths,
out_file=glue("{out_dir}/mean_depth_per_marker.tsv")
)
compute_mean_depth_per_marker(
depths=depths,
out_file=glue("{out_dir}/mean_depth_per_marker.tsv")
)
load_all(".")
compute_mean_depth_per_marker(
depths=depths,
out_file=glue("{out_dir}/mean_depth_per_marker.tsv")
)
compute_mean_depth_per_marker(
depths=depths,
#out_file=glue("{out_dir}/mean_depth_per_marker.tsv")
)
untruc<-compute_mean_depth_per_marker(
depths=depths,
#out_file=glue("{out_dir}/mean_depth_per_marker.tsv")
)
View(untruc)
genotypeFreqEstimation_path="~/GitHub/mip-poolfreq/scripts/genotypeFreqEstimation"
genotyping_vcf_path=paste0(genotypeFreqEstimation_path, "/data_test/data/GENOTYPING_MATRIX/04__Genotype_Locus1_Sample_Locus2_Filtered.vcf")
lib_names_corresp_path=paste0(genotypeFreqEstimation_path, "/data_test/data/infos_files/corresp_comp_genotypes_libnames.tsv")
out_dir=paste0(genotypeFreqEstimation_path, "/data_test/results/plot_MAF_hist")
## Import dependencies
user_lib <- Sys.getenv("R_LIBS_USER")
if (!dir.exists(user_lib)) dir.create(user_lib, recursive = TRUE)
.libPaths(user_lib)
pkgs<-c(
"optparse",
"devtools",
"this.path",
"vcfR",
"glue",
"UpSetR"
)
for (pkg in pkgs) {
if (!requireNamespace(pkg, quietly = TRUE)) {
message("Installing missing package: ", pkg)
install.packages(pkg, dependencies = TRUE)
}
library(pkg, character.only = TRUE)
}
script_dir <- dirname(this.path::this.path())
project_root <- normalizePath(file.path(script_dir, ".."))
setwd(project_root)
load_all(".")
load_all(".")
required_files <- list(
vcf = genotyping_vcf_path,
out_dir = out_dir
)
optional_files <- list(
libs = lib_names_corresp_path
)
check_missing_args(args = required_files)
check_input_files(
required_files = required_files,
optional_files = optional_files
)
## Import input file
vcf <- read.vcfR(genotyping_vcf_path)
majmin_gt_matrix<-vcf_to_majmin_num_gt_matrix(
vcf_path = genotyping_vcf_path,
lib_names_corresp_path = lib_names_corresp_path
)
## Analysis
plot_MAF_hist(
vcf = vcf,
x_lim_values = c(0, 0.5),
#out_dir = out_dir
#out_file = glue("{out_dir}/MAF_hist.png")
)
load_all(".")
## Analysis
plot_MAF_hist(
vcf = vcf,
x_lim_values = c(0, 0.5),
#out_dir = out_dir
#out_file = glue("{out_dir}/MAF_hist.png")
)
#' Plot histogram of Minor Allele Frequencies (MAF) from a VCF object
#'
#' This function extracts the minor allele frequencies (MAF) from a `vcfR` object
#' and plots a histogram of their distribution.
#'
#' @param vcf A `vcfR` object, as read by [vcfR::read.vcfR()].
#' @param x_lim_values A numeric vector of length 2 indicating the minimum and maximum x-axis limits (e.g., `c(0, 0.5)`).
#'
#' @return A histogram plot of the MAF distribution. No return value; the function is used for its side effect (plot).
plot_MAF_hist<-function(vcf, x_lim_values, out_dir = NULL){
## Analysis
plot_MAF_hist(
vcf = vcf,
x_lim_values = c(0, 0.5),
#out_dir = out_dir
#out_file = glue("{out_dir}/MAF_hist.png")
)
## Analysis
plot_MAF_hist(
vcf = vcf,
x_lim_values = c(0, 0.5),
#out_dir = out_dir
#out_file = glue("{out_dir}/MAF_hist.png")
)
#' Plot histogram of Minor Allele Frequencies (MAF) from a VCF object
#'
#' This function extracts the minor allele frequencies (MAF) from a `vcfR` object
#' and plots a histogram of their distribution.
#'
#' @param vcf A `vcfR` object, as read by [vcfR::read.vcfR()].
#' @param x_lim_values A numeric vector of length 2 indicating the minimum and maximum x-axis limits (e.g., `c(0, 0.5)`).
#'
#' @return A histogram plot of the MAF distribution. No return value; the function is used for its side effect (plot).
plot_MAF_hist<-function(vcf, x_lim_values, out_dir = NULL){
## Analysis
plot_MAF_hist(
vcf = vcf,
x_lim_values = c(0, 0.5),
#out_dir = out_dir
#out_file = glue("{out_dir}/MAF_hist.png")
)
#' Plot histogram of Minor Allele Frequencies (MAF) from a VCF object
#'
#' This function extracts the minor allele frequencies (MAF) from a `vcfR` object
#' and plots a histogram of their distribution.
#'
#' @param vcf A `vcfR` object, as read by [vcfR::read.vcfR()].
#' @param x_lim_values A numeric vector of length 2 indicating the minimum and maximum x-axis limits (e.g., `c(0, 0.5)`).
#'
#' @return A histogram plot of the MAF distribution. No return value; the function is used for its side effect (plot).
plot_MAF_hist<-function(vcf, x_lim_values, out_dir = NULL){
## Analysis
plot_MAF_hist(
vcf = vcf,
x_lim_values = c(0, 0.5),
#out_dir = out_dir
#out_file = glue("{out_dir}/MAF_hist.png")
)
#' Plot histogram of Minor Allele Frequencies (MAF) from a VCF object
#'
#' This function extracts the minor allele frequencies (MAF) from a `vcfR` object
#' and plots a histogram of their distribution.
#'
#' @param vcf A `vcfR` object, as read by [vcfR::read.vcfR()].
#' @param x_lim_values A numeric vector of length 2 indicating the minimum and maximum x-axis limits (e.g., `c(0, 0.5)`).
#'
#' @return A histogram plot of the MAF distribution. No return value; the function is used for its side effect (plot).
plot_MAF_hist<-function(vcf, x_lim_values, out_dir = NULL){
## Analysis
plot_MAF_hist(
vcf = vcf,
x_lim_values = c(0, 0.5),
#out_dir = out_dir
#out_file = glue("{out_dir}/MAF_hist.png")
)
#' Plot histogram of Minor Allele Frequencies (MAF) from a VCF object
#'
#' This function extracts the minor allele frequencies (MAF) from a `vcfR` object
#' and plots a histogram of their distribution.
#'
#' @param vcf A `vcfR` object, as read by [vcfR::read.vcfR()].
#' @param x_lim_values A numeric vector of length 2 indicating the minimum and maximum x-axis limits (e.g., `c(0, 0.5)`).
#'
#' @return A histogram plot of the MAF distribution. No return value; the function is used for its side effect (plot).
plot_MAF_hist<-function(vcf, x_lim_values, out_dir = NULL){
## Analysis
plot_MAF_hist(
vcf = vcf,
x_lim_values = c(0, 0.5),
#out_dir = out_dir
#out_file = glue("{out_dir}/MAF_hist.png")
)
# v ... to replace with CLI params ... v
## Input parameters
genotypeFreqEstimation_path="~/GitHub/mip-poolfreq/scripts/genotypeFreqEstimation"
genotyping_vcf_path=paste0(genotypeFreqEstimation_path, "/data_test/data/GENOTYPING_MATRIX/04__Genotype_Locus1_Sample_Locus2_Filtered.vcf")
lib_names_corresp_path=paste0(genotypeFreqEstimation_path, "/data_test/data/infos_files/corresp_comp_genotypes_libnames.tsv")
out_dir=paste0(genotypeFreqEstimation_path, "/data_test/results/plot_MAF_hist")
## Import dependencies
user_lib <- Sys.getenv("R_LIBS_USER")
if (!dir.exists(user_lib)) dir.create(user_lib, recursive = TRUE)
.libPaths(user_lib)
pkgs<-c(
"optparse",
"devtools",
"this.path",
"vcfR",
"glue",
"UpSetR"
)
for (pkg in pkgs) {
if (!requireNamespace(pkg, quietly = TRUE)) {
message("Installing missing package: ", pkg)
install.packages(pkg, dependencies = TRUE)
}
library(pkg, character.only = TRUE)
}
script_dir <- dirname(this.path::this.path())
project_root <- normalizePath(file.path(script_dir, ".."))
setwd(project_root)
load_all(".")
required_files <- list(
vcf = genotyping_vcf_path,
out_dir = out_dir
)
optional_files <- list(
libs = lib_names_corresp_path
)
check_missing_args(args = required_files)
check_input_files(
required_files = required_files,
optional_files = optional_files
)
## Import input file
vcf <- read.vcfR(genotyping_vcf_path)
majmin_gt_matrix<-vcf_to_majmin_num_gt_matrix(
vcf_path = genotyping_vcf_path,
lib_names_corresp_path = lib_names_corresp_path
)
## Analysis
plot_MAF_hist(
vcf = vcf,
x_lim_values = c(0, 0.5),
#out_dir = out_dir
#out_file = glue("{out_dir}/MAF_hist.png")
)
## Import input file
vcf <- read.vcfR(genotyping_vcf_path)
